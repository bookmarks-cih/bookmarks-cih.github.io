# GitHub Pages

Ce repo est volontairement vierge et indépendant du dépôt principal `bookmarks-cih/Bookmarks`.
Il contient seulement une interface statique de génération de blocs de code pour administrer manuellement `bookmarks.love`.
